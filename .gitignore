# --- Data: never commit. Afrobarometer data is not ours to redistribute. ---
data/raw/*
!data/raw/.gitkeep
data/staging/*
!data/staging/.gitkeep
data/*.duckdb
data/*.duckdb.wal
*.sav
*.parquet
# Afrobarometer codebooks are copyrighted; do not redistribute
*.pdf

# --- R ---
.Rhistory
.RData
.Rproj.user/
.Renviron

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/

# --- Private planning docs: not for a public portfolio repo ---
afrobarometer_sql_project_outline.md
sql_project_strategy.md

# scratch dir created by the remote session (safe to delete)
_to_delete/

# Derived from the raw data and codebooks; regenerate with R/01 and R/02.
# codebook_entries.csv holds ~1,445 verbatim question texts parsed out of the
# Afrobarometer codebook PDFs. Those are copyrighted publications, so the parse
# is not redistributed here -- only the 64 crosswalk rows the analysis documents.
docs/inventory_*.csv
docs/codebook_entries.csv
docs/staging_manifest.csv

# NOTE: output/ and figures/ ARE committed, deliberately. They are small, and
# they are the provenance for every number in docs/memo.md -- a reader can check
# the memo without rebuilding the database. Regenerate with R/06 and R/07.
